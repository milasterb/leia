Drop `og-image.png` here (the same thumbnail you used for the Devpost
gallery works great — ideally ~1200x630 or your existing 3:2 hero shot).
Vite serves this folder at the site root, so it becomes reachable at
https://leia-theta.vercel.app/og-image.png, which is what the Open Graph
meta tags in index.html point to.
